---
description: Show Codex subscription usage from Brave
---
Show the current Codex subscription usage by reading the logged-in Brave browser session.

Result:
!`node "$HOME/.config/opencode/scripts/codex-usage.mjs"`

Reply with only the result.
