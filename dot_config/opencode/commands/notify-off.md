---
description: Disable OpenCode notifications
---
Disable global OpenCode notifications.

Result:
!`mkdir -p "$HOME/.config/opencode" && touch "$HOME/.config/opencode/notify.off" && echo "Notifications: OFF"`

Reply with only the result line.
