---
description: Enable OpenCode notifications
---
Enable global OpenCode notifications.

Result:
!`rm -f "$HOME/.config/opencode/notify.off" && echo "Notifications: ON"`

Reply with only the result line.
